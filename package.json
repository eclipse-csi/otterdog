{
  "name": "otterdog",

  "main": "src/test.ts",
  "scripts": {
    "build": "npx tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "puppeteer": "^17.0.0",
    "ts-command-line-args": "^2.3.1"
  },
  "devDependencies": {
    "@types/node": "^18.7.15",
    "typescript": "^4.8.2"
  }
}
